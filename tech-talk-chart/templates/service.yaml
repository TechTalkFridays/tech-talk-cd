apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.name }}
  name: {{ .Release.name }}
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: http-flask
    port: 80
    targetPort: 5000
  selector:
    app: {{ .Release.name }}
  sessionAffinity: ClientIP
